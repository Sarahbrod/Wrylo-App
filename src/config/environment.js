const config = {
  // Use your local network IP for Expo to connect from physical devices/simulators
  API_BASE_URL: 'http://10.0.0.156:8000/api',
  ENCRYPTION_KEY: 'wrylo-encryption-key-2024',
};

export default config;